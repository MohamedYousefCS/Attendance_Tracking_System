@model Student;
@{
    ViewData["Title"] = "showAttendance";
    Layout = "~/Views/Shared/MainLayout.cshtml";
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
    <!-- jQuery -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <link rel="stylesheet" href="/css/AttendStudent.css" />
</head>

<body>
    <div class="container mt-5 px-2">

        <div class="containerInfo">
            <div style="border-radius:11px;display:flex;padding:17px;width:33%;justify-items:center;justify-content:space-around;" class="alert alert-danger">
                @ViewBag.absentDays Absent Days
            </div>
            <div style="border-radius:11px;display:flex;padding:17px;width:33%;justify-items:center;justify-content:space-around;" class="alert alert-warning">
                @ViewBag.lateDays late Days
            </div>
            <div style="border-radius:11px;display:flex;padding:17px;width:33%;justify-items:center;justify-content:space-around;" class="alert alert-info">
              CurrentDegree   @ViewBag.tatalDegree
            </div>
        </div>



        <form id="attendanceForm">
            <div class="row">
                <div class="col">
                    <label for="date">Choose a date from:</label>
                    <input type="date" id="date" name="date">
                </div>
                <div class="col">
                    <label for="date2">to:</label>
                    <input type="date" id="date2" name="date2">
                </div>
                <div class="col" style="text-align:end">
                    <button type="submit" class="btn btn-primary">Go</button>
                </div>
            </div>
        </form>


        <div id="attendanceTableContainer"></div>

        @section scripts {
            <script>
                $(document).ready(function () {
                    // Get today's date
                    var today = new Date();
                    // Format today's date as YYYY-MM-DD (required format for input type=date)
                    var formattedDate = today.toISOString().substr(0, 10);
                    // Set the value of the 'date2' input field to today's date
                    $("#date2").val(formattedDate);

                    $("#attendanceForm").submit(function (e) {
                        e.preventDefault(); // Prevent form submission
                        var formData = $(this).serialize(); // Serialize form data
                        $.ajax({
                            url: "@Url.Action("AttendanceData", "Student")", // Replace with your controller action
                            type: "GET",
                            data: formData,
                            success: function (result) {
                                $("#attendanceTableContainer").html(result); // Update the partial view
                            },
                            error: function () {
                                alert("An error occurred while fetching attendance data.");
                            }
                        });
                    });
                });
            </script>
        }


        @*
        <form id="attendanceForm">
            <div class="row">
                <div class="col">
                    <label for="date">Choose a date from:</label>
                    <input type="date" id="date" name="date">
                </div>
                <div class="col">
                    <label for="date2">to:</label>
                    <input type="date" id="date2" name="date2">
                </div>
                <div class="col " style="text-align:end">
                    <button type="submit" class="btn btn-primary">Go</button>
                </div>
            </div>
        </form>

        <div class="table-responsive DunamicTable" style="display:none">
            <table class="table table-responsive table-borderless ">

                <thead>
                    <tr class="bg-light">
                        <th>Date</th>
                        <th>Status</th>
                        <th>Ispermission</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Attendances.Count() == 0)
                    {
                        <tr>
                            <td colspan="4" style="text-align:center">معندكش غياب والله لسه ...شكلك جديد عندنا </td>
                        </tr>
                    }
                    else
                    {
                        foreach (var day in Model.Attendances)
                        {
                            <tr>
                                <td>@day.Date</td>
                                @if (day.Status == 0)
                                {
                                    <td><i class="fa fa-check-circle-o green"></i><span class="ms-1">Present</span></td>
                                }
                                else if ((int)day.Status == 1)
                                {
                                    <td><i class="fa fa-dot-circle-o text-danger"></i><span class="ms-1">Absent</span></td>
                                }
                                else
                                {
                                    <td><i class="fa fa-dot-circle-o text-warning"></i><span class="ms-1">Late</span></td>
                                }

                                @if (day.PermissionType == null && day.IsPermission == null)
                                {
                                    <td><i class="fa-solid fa-window-minimize"></i></td>
                                }
                                else if (day.PermissionType == PermissionType.Excused || day.IsPermission == true)
                                {
                                    <td>yes</td>
                                }
                                else if (day.IsPermission == null || day.IsPermission == false)
                                {
                                    <td>no</td>
                                }
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
    <script>
        $(document).ready(function () {
            $('#attendanceForm').submit(function (e) {
                e.preventDefault(); // Prevent form submission
                $('.DunamicTable').show(); // Show the table
            });
        });
    </script>
    *@
</body>

</html>
